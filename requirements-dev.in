# Used to create the requirements-dev.txt file
# Note: `pip-compile requirements-dev.in` needs to be run before changes in requirements-dev.in are reflected in
# requirements-dev.txt, which is used for GitHub Actions
black==22.3.0 # This should be kept in sync with the version in .pre-commit-config.yaml
isort==5.10.1 # This should be kept in sync with the version in .pre-commit-config.yaml
pre-commit
pydocstyle==6.0.0 # This should be kept in sync with the version in .pre-commit-config.yaml
pylint
pytest
